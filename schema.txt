-- Determine the relations for Project 3 assignment


top_artists
-
nameP           VARCHAR(50) 
colloq_name     VARCHAR(50)
last_name       VARCHAR(50)
lifespanP       VARCHAR(20)
separator       DECIMAL
bio             VARCHAR(750)

carnegie
-
last_name        VARCHAR(50)   
cited_name       VARCHAR(100)
artwork_genre    VARCHAR(50)
title            VARCHAR(450)
artist           VARCHAR(100) FK >- top_artists.nameP
creation_date    VARCHAR(50)
nationality      VARCHAR(50)
index            DECIMAL
museum           VARCHAR(100)

cleveland
-
artist           VARCHAR(100) 
title            VARCHAR(255)
museum           VARCHAR(100)
artwork_genre    VARCHAR(50)
creation_date    VARCHAR(100)

chicago
-
artist           VARCHAR(100) 
title            VARCHAR(255)
museum           VARCHAR(100)
artwork_genre    VARCHAR(50)
creation_date    VARCHAR(100)


